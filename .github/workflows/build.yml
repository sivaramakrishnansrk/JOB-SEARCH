name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Prepare Wrapper and Build Directory
        run: |
          # Detect nested folder if project files are inside MechJobRadar or JobTrackerApp
          if [ -d "MechJobRadar" ]; then
            cd MechJobRadar
          elif [ -d "JobTrackerApp" ]; then
            cd JobTrackerApp
          fi
          
          # Generate gradlew wrapper if not committed to repository
          if [ ! -f "gradlew" ]; then
            gradle wrapper
          fi
          
          chmod +x gradlew

      - name: Build Debug APK
        run: |
          if [ -d "MechJobRadar" ]; then
            cd MechJobRadar
          elif [ -d "JobTrackerApp" ]; then
            cd JobTrackerApp
          fi
          ./gradlew assembleDebug --stacktrace

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: "**/build/outputs/apk/debug/*.apk"
          retention-days: 14
          
